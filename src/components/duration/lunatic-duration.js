import Duration from './duration';
import { createLunaticComponent } from '../commons';

const LunaticDuration = createLunaticComponent(Duration, {
	inputId: 'lunatic-duration',
});

export default LunaticDuration;
